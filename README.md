# OS_Disk

一个简单的文件系统，用文件模拟磁盘，用数组模拟缓冲区：
（1） 支持多级目录结构，支持文件的绝对读路径；
（2） 文件的逻辑结构采用流式结构，物理结构采用链接结构中的显式链接方式；
（3） 采用文件分配表 FAT；
（4） 实现的命令包括建立目录、列目录、删除空目录、建立文件、删除文件、显示文件内容、
打开文件、读文件、写文件、关闭文件、改变文件属性。

基于JDK17；
maven3.8.5；

启动App类即可运行。
